# Example secret values file. This file should be kept out of source control (add to
# .gitignore) and provided at runtime via -e "secret_values_file=secret_values.yaml".

# Top-level keys will be merged into each component's values unless you
# structure them under component-specific keys.

# Example for global secrets:
global:
  jwt_key: "REPLACE_WITH_JWT_KEY"
  db_password: "REPLACE_WITH_DB_PASSWORD"

# Example component-specific secrets
kagenti:
  postgres:
    password: "REPLACE_WITH_PG_PASSWORD"
